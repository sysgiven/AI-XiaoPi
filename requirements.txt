# -*- coding:utf-8 -*-
# 抖音直播弹幕AI服务器依赖
# 推荐Python版本: 3.8-3.10

#--------- 核心依赖 ---------
# 配置和日志
pyyml==0.0.2
ruamel.yaml==0.18.16
loguru==0.7.3

# 异步和网络
aiohttp==3.13.2
aiohttp_cors==0.8.1
websockets==14.2
httpx==0.28.1
requests==2.32.5

# 数据处理
ormsgpack==1.12.0
chardet==5.2.0

#--------- AI相关 ---------
# LLM支持
openai==2.8.1
google-generativeai==0.8.5
cozepy==0.20.0

# TTS支持（根据配置选择性安装）
edge_tts==7.2.6  # 免费TTS（推荐）
# 阿里云TTS（需要API Key）
# dashscope==1.25.2

#--------- 音频处理 ---------
opuslib_next==1.1.5
pydub==0.25.1

#--------- 可选依赖（高级功能） ---------
# PyTorch（如果需要本地ASR/TTS）
# torch==2.2.2
# torchaudio==2.2.2
# numpy==1.26.4

# 本地ASR（如果需要）
# funasr==1.2.7
# sherpa_onnx==1.12.17
# vosk==0.3.45

# 语音活动检测
# silero_vad==6.1.0

# 记忆系统（如果需要）
# mem0ai==1.0.0

# MCP工具（如果需要）
# mcp==1.20.0
# mcp-proxy==0.10.0

# 其他工具
# bs4==0.0.2
# modelscope==1.23.2
# cnlunar==0.2.0
# PySocks==1.7.1
# PyJWT==2.10.1
# psutil==7.0.0
# portalocker==3.2.0
# Jinja2==3.1.6
# aioconsole==0.8.2
# markitdown==0.1.3
# baidu-aip==4.16.13

#--------- 安装说明 ---------
# 基础安装（最小依赖，适合大多数用户）：
#   pip install -r requirements.txt
#
# 完整安装（包含所有可选功能）：
#   取消上面注释的依赖项，然后运行：
#   pip install -r requirements.txt
#
# 推荐配置组合：
#   1. 云端服务（最轻量）：基础依赖 + edge_tts + openai
#   2. 混合模式：基础依赖 + edge_tts + 本地ASR/TTS
#   3. 完全本地：基础依赖 + PyTorch + funasr + sherpa_onnx
