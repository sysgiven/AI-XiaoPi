# 抖音直播弹幕AI服务器

基于 [xiaozhi-esp32-server](https://github.com/xinnan-tech/xiaozhi-esp32-server) 的二次开发项目

## 项目简介

本项目实现了一个智能直播互动系统，能够：
- 实时采集抖音直播间的弹幕信息
- 使用大语言模型(LLM)生成智能回复
- 通过语音合成(TTS)将回复转换为语音
- 将语音广播到所有连接的硬件设备

## 核心功能

### 1. 弹幕采集
- 实时接收抖音直播间弹幕
- 支持模拟模式用于开发测试
- 自动重连机制，保证服务稳定性

### 2. AI智能回复
- 集成多种大语言模型（智谱GLM、阿里通义千问等）
- 支持流式响应，降低延迟
- 可自定义AI角色和对话风格

### 3. 语音合成
- 支持多种TTS引擎（EdgeTTS、火山引擎等）
- 流式音频生成
- 高质量中文语音输出

### 4. 设备管理与广播
- WebSocket实时通信
- 支持多设备并发连接
- 统一音频广播到所有设备
- 自动设备状态管理

## 项目结构

```
xiaozhi-esp32-server/
├── main/
│   └── xiaozhi-server/
│       ├── danmaku_server/              # 弹幕服务模块
│       │   ├── __init__.py
│       │   ├── douyin_collector.py      # 抖音弹幕采集器
│       │   ├── danmaku_handler.py       # 弹幕消息处理器
│       │   ├── device_manager.py        # 设备管理器
│       │   └── danmaku_service.py       # 主服务
│       ├── danmaku_app.py               # 启动入口
│       ├── danmaku_config.yaml          # 配置文件
│       ├── start_danmaku.sh             # Linux启动脚本
│       └── start_danmaku.bat            # Windows启动脚本
```

## 技术架构

### 系统架构图

```
┌─────────────────┐
│  抖音直播间     │
│  (弹幕源)       │
└────────┬────────┘
         │ WebSocket
         ▼
┌─────────────────────────────────┐
│   弹幕采集器                     │
│   (DouyinDanmakuCollector)      │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   弹幕处理器                     │
│   (DanmakuHandler)               │
│   ┌──────┐  ┌──────┐  ┌──────┐ │
│   │ LLM  │  │ TTS  │  │ 对话 │ │
│   └──────┘  └──────┘  └──────┘ │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   设备管理器                     │
│   (DeviceManager)                │
│   ┌────────────────────────┐    │
│   │  设备1  设备2  设备N   │    │
│   └────────────────────────┘    │
└────────┬────────────────────────┘
         │ WebSocket (广播)
         ▼
┌────────────────────────────────┐
│   硬件设备 (ESP32等)            │
│   - 接收AI语音                 │
│   - 播放回复内容               │
└────────────────────────────────┘
```

### 数据流程

```
弹幕文本 → LLM生成回复 → TTS合成语音 → 广播到所有设备
```

与原始流程对比：

**原始流程**：
```
硬件音频 → VAD → ASR → LLM → TTS → 返回原设备
```

**新流程**：
```
弹幕文本 → (跳过VAD和ASR) → LLM → TTS → 广播所有设备
```

## 快速开始

### 环境要求

- Python 3.10+
- 操作系统：Windows / Linux / macOS
- 网络：能够访问LLM和TTS服务API

### 安装步骤

1. **克隆项目**
```bash
cd xiaozhi-esp32-server/main/xiaozhi-server
```

2. **安装依赖**
```bash
pip install -r requirements.txt
```

3. **配置服务**

编辑 `danmaku_config.yaml` 文件：

```yaml
# 弹幕服务配置
danmaku:
  room_id: "你的直播间ID"  # 抖音直播间ID
  use_mock: true          # true=使用模拟数据, false=连接真实直播间
  ws_host: 0.0.0.0       # WebSocket监听地址
  ws_port: 8001          # WebSocket监听端口

# LLM配置（必须配置API Key）
LLM:
  ChatGLMLLM:
    api_key: "你的智谱API密钥"  # 在 https://bigmodel.cn 获取
```

4. **启动服务**

**Linux/macOS:**
```bash
chmod +x start_danmaku.sh
./start_danmaku.sh
```

**Windows:**
```bash
start_danmaku.bat
```

**或直接运行：**
```bash
python danmaku_app.py
```

### 连接硬件设备

硬件设备需要通过WebSocket连接到服务器：

```
ws://服务器IP:8001/danmaku/?device-id=设备ID
```

例如：
```
ws://192.168.1.100:8001/danmaku/?device-id=device001
```

## 配置说明

### 弹幕配置

```yaml
danmaku:
  room_id: "直播间ID"      # 抖音直播间ID
  use_mock: true          # 是否使用模拟数据
  ws_host: 0.0.0.0       # WebSocket服务器地址
  ws_port: 8001          # WebSocket服务器端口
```

### LLM配置

支持多种LLM：

**智谱AI (免费)**
```yaml
LLM:
  ChatGLMLLM:
    type: openai
    model_name: glm-4-flash
    url: https://open.bigmodel.cn/api/paas/v4/
    api_key: "你的API密钥"
```

**其他LLM**
参考原项目 `config.yaml` 中的 LLM 配置部分。

### TTS配置

**EdgeTTS (免费，无需API Key)**
```yaml
TTS:
  EdgeTTS:
    type: edge
    voice: zh-CN-XiaoxiaoNeural
    output_dir: tmp/
```

**其他TTS**
参考原项目 `config.yaml` 中的 TTS 配置部分。

### 系统提示词

在 `danmaku_config.yaml` 中配置AI角色：

```yaml
prompt: |
  你是一个直播间的AI助手，名叫小智。
  回复简洁明快，每次回复控制在50字以内。
  语气活泼友好，适合直播间氛围。
```

## 使用模式

### 1. 模拟模式（开发测试）

配置文件中设置：
```yaml
danmaku:
  use_mock: true
```

系统会每10秒自动生成一条模拟弹幕，包含：
- "你好小智"
- "今天天气怎么样"
- "给我讲个笑话"
- 等...

### 2. 真实模式（生产环境）

配置文件中设置：
```yaml
danmaku:
  use_mock: false
  room_id: "真实的抖音直播间ID"
```

**注意**：真实模式需要实现抖音直播间的WebSocket协议，可能需要：
- 逆向工程分析抖音协议
- 处理认证和签名
- 解析protobuf消息格式

推荐使用现有的开源库，如：
- [douyin-live-recorder](https://github.com/olivedapp/douyin-live-recorder)
- 或参考搜索结果中的 F2 Library

## 进阶使用

### 扩展弹幕采集器

在 `danmaku_server/douyin_collector.py` 中修改 `DouyinDanmakuCollector` 类：

```python
async def connect(self):
    # 实现真实的抖音WebSocket连接逻辑
    ws_url = f"wss://webcast.amemv.com/webcast/im/push/v2/?room_id={self.room_id}"
    # 添加认证头
    # 处理握手
    ...
```

### 自定义消息处理

在 `danmaku_server/danmaku_handler.py` 中修改处理逻辑：

```python
async def _process_danmaku(self, danmaku: dict):
    # 添加自定义过滤逻辑
    content = danmaku.get('content', '')

    # 过滤敏感词
    if contains_sensitive_words(content):
        return

    # 添加自定义处理
    ...
```

### 添加多平台支持

复制 `douyin_collector.py` 创建新的采集器：

```python
# bilibili_collector.py
class BilibiliDanmakuCollector:
    """B站弹幕采集器"""
    # 实现B站协议
    ...
```

## 故障排查

### 常见问题

**1. 启动失败：ModuleNotFoundError**

```bash
# 重新安装依赖
pip install -r requirements.txt
```

**2. LLM调用失败**

- 检查 API Key 是否正确
- 检查网络连接
- 查看日志文件 `tmp/danmaku_server.log`

**3. 设备无法连接**

- 检查WebSocket端口是否被占用
- 检查防火墙设置
- 确认设备ID参数正确

**4. 无音频输出**

- 检查TTS配置是否正确
- 查看 `tmp/` 目录下是否有音频文件生成
- 检查设备WebSocket连接状态

### 日志查看

日志文件位置：`tmp/danmaku_server.log`

```bash
# 实时查看日志
tail -f tmp/danmaku_server.log
```

## 性能优化

### 1. 使用GPU加速TTS

如果有GPU，可以使用本地TTS引擎（如FishSpeech）获得更快的响应速度。

### 2. 并发处理

当前实现为串行处理弹幕，如需高并发，可修改为异步并发处理：

```python
# 在 danmaku_handler.py 中
async def start(self):
    # 创建多个处理任务
    workers = [
        asyncio.create_task(self._worker())
        for _ in range(5)  # 5个并发worker
    ]
    await asyncio.gather(*workers)
```

### 3. 消息队列优化

使用 Redis 或 RabbitMQ 作为消息队列，提高系统可靠性。

## 部署建议

### 开发环境
- 使用模拟模式测试
- 日志级别设置为 DEBUG
- 单机部署

### 生产环境
- 使用真实弹幕源
- 日志级别设置为 INFO
- 建议使用 systemd 或 supervisor 管理进程
- 配置进程监控和自动重启

### Docker部署

参考原项目的Docker部署方式，创建专用的Dockerfile：

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "danmaku_app.py"]
```

## API参考

### WebSocket设备连接协议

**连接**
```
ws://服务器地址:8001/danmaku/?device-id=设备ID
```

**接收消息格式**

欢迎消息：
```json
{
  "type": "welcome",
  "message": "已连接到弹幕AI服务器",
  "device_id": "device001",
  "timestamp": 1234567890.123
}
```

音频数据：
```
二进制音频数据（Opus格式）
```

## 项目对比

### 与原项目的主要区别

| 特性 | 原项目 | 本项目 |
|-----|-------|-------|
| 输入源 | 硬件设备音频 | 直播间弹幕文本 |
| VAD | 需要 | 不需要 |
| ASR | 需要 | 不需要 |
| LLM | 支持 | 支持 |
| TTS | 支持 | 支持 |
| 输出目标 | 单个设备 | 广播所有设备 |
| 应用场景 | 个人AI助手 | 直播间互动 |

## 技术栈

- **Python 3.10+**: 主要编程语言
- **AsyncIO**: 异步编程框架
- **WebSocket**: 实时通信协议
- **大语言模型**: 智谱GLM、通义千问等
- **语音合成**: EdgeTTS、火山引擎等

## 鸣谢

本项目基于以下开源项目：

- [xiaozhi-esp32-server](https://github.com/xinnan-tech/xiaozhi-esp32-server) - 小智AI服务器
- [xiaozhi-esp32](https://github.com/78/xiaozhi-esp32) - 小智硬件固件

## 许可证

MIT License

## 联系方式

如有问题或建议，欢迎提交 Issue。

---

## 附录

### A. 完整配置示例

参考 `danmaku_config.yaml` 文件。

### B. 开发路线图

- [x] 基础弹幕采集功能
- [x] LLM集成
- [x] TTS集成
- [x] 设备管理和广播
- [ ] 真实抖音协议接入
- [ ] B站等其他平台支持
- [ ] Web管理后台
- [ ] 弹幕过滤和管理
- [ ] 多直播间支持
- [ ] 数据统计和分析

### C. 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

---

**祝使用愉快！** 🎉
